// const { default: MainSlider } = require("@/Components/MainSlider");
const { createContext } = require("react");

const Context = createContext({
  MainSliderImage: [],
});

export default Context;
